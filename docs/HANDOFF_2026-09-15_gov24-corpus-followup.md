# 인수인계서 — gov24_corpus 후속 작업 (2026-09-15)

**전제**: `SESSION_SUMMARY_GOV24_CORPUS_FULL_VERIFICATION_20260915_v1_0.md`(같은 개발자 문서 메뉴, 상세 원인·조치 전부 기록됨). 이 문서는 다음 세션이 바로 시작할 수 있게 요점만 압축한 것.

## 상태
gov24_corpus 350건(batch1~7) 전수 라이브 재검증 완료. 크래시 0, 응급감지 0. 오늘 반영된 것:
- `SP-NAT-GOVPLATFORM` 신설 (범부처 자체서비스 — 하이패스 미납요금·요금감면일괄·미환급금·지원결정통지 출력·1372 소비자상담센터)
- 키워드/구조 수정 7건 (검정고시·보험가입증명원·표준재무제표증명원 키워드 추가, 어린이집→도서관 오분류, 관세/국세 동점충돌, 전세자금대출→한국은행 오분류)
- `gov24_corpus_live_smoketest.mjs`에 20초×3회 재시도 로직 도입(무한대기 장애 대응)

## 다음 세션이 할 일 — 우선순위 순
1. **식품영업 부서 4-way 분산** — "식품관련영업신고"류가 WELFARE/BOHWAN/SAFETY로 매번 다르게 감. do-dept 후보 중 하나로 명시적 고정 검토.
2. **LABOR vs WELFARE 휴가제도 분산** — 가족돌봄휴직(LABOR) vs 육아휴직급여/출산전후휴가(WELFARE) vs 생리휴가(LABOR). 고용보험 급여 계열은 LABOR로 통일 제안.
3. **"가격확인서" 계열 3-way 분산** — 개별공시지가(시청건축과)/개별주택가격(PLAN)/주택가격동향(HOUSING).
4. batch1~7 재실행해서 오늘 고친 7건이 회귀 없이 재현되는지 확인.
5. 확신도 중간 개별 건들(국방 X 계열 EMD 낙착, 나이스/RISS/대입정보포털 EMD 낙착, b7-045 휴업폐업신고, b5-002/012/038 등) — 재현되는지 보고 키워드 보강 여부 결정.

## 주의사항
- `git apply`를 하위 폴더(`tests\live_smoketest` 등)에서 실행하면 그 폴더 밖 파일에 대한 패치는 **에러 없이 조용히 건너뛴다** — 반드시 저장소 루트에서 적용할 것. `git status`로 `modified:` 파일이 실제로 잡히는지 매번 확인.
- 라이브 스모크테스트 도중 응답이 계속 없으면(재시도 3회 다 실패) DeepSeek/네트워크 쪽 문제일 수 있음 — 코드보다 `wrangler tail hondi-proxy`로 실측부터. (자세한 진단 절차는 SESSION_SUMMARY 문서 2절 참고)
