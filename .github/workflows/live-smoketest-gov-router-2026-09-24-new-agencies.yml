name: Live Smoketest — gov-tree 신설 SP 18개(batch12~17) 라우팅 (실제 DeepSeek)

on:
  workflow_dispatch: {}

permissions:
  contents: write

jobs:
  run-new-agencies-routing-smoketest:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Run new-agencies routing + SP response live smoketest
        working-directory: tests/live_smoketest
        env:
          DEEPSEEK_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
        run: node gov_router_2026_09_24_new_agencies_live_smoketest.mjs

      - name: Commit results to results branch
        if: always()
        run: |
          git config user.name "hondi-live-smoketest-bot"
          git config user.email "actions@users.noreply.github.com"
          git checkout -B results/live-smoketest-gov-router-2026-09-24-new-agencies
          git add -f results/
          git diff --cached --quiet || git commit -m "chore: 2026-09-24 신설 SP 라우팅 live smoketest results ($(date -u +%Y-%m-%dT%H:%M:%SZ))"
          git push --force origin HEAD:results/live-smoketest-gov-router-2026-09-24-new-agencies

      - name: Upload artifact (backup)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: live-smoketest-gov-router-2026-09-24-new-agencies-results
          path: results/
