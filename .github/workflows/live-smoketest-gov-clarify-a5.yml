name: Live Smoketest — Gov Router CLARIFY A5 (National Agency ↔ Local Collision)

on:
  workflow_dispatch: {}

permissions:
  contents: write

jobs:
  run-gov-clarify-a5-smoketest:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install deps
        run: pip install requests

      - name: Run gov-router CLARIFY A5 (natMatch 지사형 집행기관 vs 지방행정 계층충돌, 6 scenarios — 4 adjacent-pair + 2 control)
        working-directory: tests/live_smoketest
        env:
          DEEPSEEK_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
        run: |
          python3 gov_clarify_live_smoketest.py \
            --scenarios scenarios_gov_clarify_a5_20260918.json \
            --out ../../results/gov-clarify-a5

      - name: Commit results to results branch
        if: always()
        run: |
          git config user.name "hondi-live-smoketest-bot"
          git config user.email "actions@users.noreply.github.com"
          git checkout -B results/live-smoketest-gov-clarify-a5
          git add -f results/
          git diff --cached --quiet || git commit -m "chore: gov-clarify-a5 live smoketest results ($(date -u +%Y-%m-%dT%H:%M:%SZ))"
          git push --force origin HEAD:results/live-smoketest-gov-clarify-a5

      - name: Upload artifact (backup)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: live-smoketest-gov-clarify-a5-results
          path: results/
